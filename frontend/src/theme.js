const { createMuiTheme } = require('@material-ui/core');

export const theme = createMuiTheme({
  // backgroundColor: "#447884",
  backgroundColor: "currentColor",
  text: {
    "primary": 'white'
  },
});